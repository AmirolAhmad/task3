<% if @list_item.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@list_item.errors.count, "error") %> prohibited this list_item from being saved:</h2>

    <ul>
    <% @list_item.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= f.label :name %>
  <%= f.text_field :name, id: :list_item_name %>
</div>

<div class="">
  <%= f.label :tag_id %>
  <%= f.collection_select :tag_id, @tags, :id, :name, { prompt: "-- Choose tag" }, { class: 'form-control ' } %>
</div>

<div class="actions">
  <%= f.submit %>
</div>
